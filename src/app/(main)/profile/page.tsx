import { ProfileSection } from "@/app/components/__organism";

export default function page() {
  return (
    <div className="w-full min-h-[calc(100vh-126px)] bg-white rounded-[12px] shadow-xl overflow-hidden relative">
      <ProfileSection />
    </div>
  );
}
